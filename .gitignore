# dotenv environment variable files
.env
.env.development.local
.env.test.local
.env.production.local
.env.local

# Fresh build directory
_fresh/
# Vite dep-optimization cache
.vite/
# npm + other dependencies
node_modules/
vendor/


# OpenFGA local config (generated by `deno task seed`)
fga.local.json

# Game-state SQLite DB (created + re-seeded on every backend start)
gamestate.db
gamestate.db-journal
gamestate.db-wal
gamestate.db-shm
